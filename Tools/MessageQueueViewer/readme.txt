This utility lets you view/add/delete (private) message-queues on a given server. You can also send and receive messages from a queue.

Note: Creating private queues is only allowed locally (when connected to "."; "localhost" won't work)